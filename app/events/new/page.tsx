import { NewEventInterview } from "@/components/app/NewEventInterview";

export const metadata = { title: "Nieuw evenement — Vyra" };

export default function NewEventPage() {
  return <NewEventInterview />;
}
